# Version control
.git
.gitignore

# Build artifacts — regenerated by `next build` inside the container
.next
node_modules

# Secrets — NEVER bake credentials into the image
# Pass them at runtime via --env-file or -e flags
.env
.env.*
.env.local
.env.development
.env.production

# Runtime-generated data — mount as a volume instead
logs/

# npm debug logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Docker files themselves (no need to copy into the build context)
Dockerfile
.dockerignore

# Documentation
*.md
